# Database Directory (Webserver)

When running the backend webserver in Web mode, all SQLite database files are stored and accessed from this directory. This location is used exclusively by the local webserver unless `APP_CONFIG.DB_DIRECTORY` is changed.
